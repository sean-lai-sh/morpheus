node_modules/
# Binary lockfile (legacy). Text `bun.lock` is tracked so CI `--frozen-lockfile` is real.
bun.lockb

# Runtime data — local dev volume; production hosts use a persistent volume
data/
!data/.gitkeep
*.db
*.db-wal
*.db-shm

# Secrets — never commit. Doppler injects at runtime.
.env
.env.local
.env.*.local

# Real channel config — contains your server's snowflakes. Copy channels.example.yml to get started.
config/channels.yml

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
*.swo

# Build output (we don't compile, but just in case)
dist/
build/
